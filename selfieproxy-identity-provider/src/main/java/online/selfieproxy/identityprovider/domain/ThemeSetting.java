package online.selfieproxy.identityprovider.domain;

/** The exact on-disk shape of theme.json -- see ThemeStore. Written by selfieproxy-portal only. */
public record ThemeSetting(String themeId) {
}
