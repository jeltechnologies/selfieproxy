# Selfie Proxy instructions for Claude Code
Selfie Proxy Portal to expose web applications, and network services running in your home network to the internet. 

Most Internet Service Providers introduced CGNAT in their networks for security, and for a more efficient usage of scarce IP addresses. For normal consumers CGNAT is a very good thing, as their networks are not directly connected to the internet. For network hobbyist the CGNAT is an obstacle because port forwarding stopped working. 

Selfie Proxy is extremely simple, feeling like port forwarding was in the early days of the internet. 

Selfie Proxy is not suitable for enterprise users, because it does not support high availability or load balancing. You ain't gonna this anyway as a hobbyist, and only introduces bloat and complexity. But Enterprise Users do need these features and should consider commercial, supported commercial producs like Cloudflare, Pangolin and Netbird. While these commercial products can also be as freemium for self hosters, they contain many complex enterprise features making it hard to setup for the hobbyist.   

## Keep it simple stupid (KISS principle)
- The operations portal for Selfie Proxy is kept as simple as possible, hiding complex network setup.
- Selfie Proxie contains just one user that can manage many local networks. 
- Local networks expose several web services to the internet as subdomains to the Selfie Proxie domain.
- Deployments are done in Docker.

## Portal

### Initial setup
- Set `ADMIN_PORTAL_USERNAME`/`ADMIN_PORTAL_PASSWORD` in `.env`, then start the server: `docker compose -f docker-compose-server.yaml up -d --build`.
- The portal itself (`SELFIEPROXY_SUBDOMAIN.DOMAIN`) is reachable immediately, before any agent is connected -- boringproxy-server reverse-proxies that domain directly to the admin portal container (`-portal-domain`/`-portal-port`), independent of the normal Agent/Tunnel mechanism, since the portal is where the first agent gets created.
- Log in, open the Agents page, find the `default` agent created automatically on first boot, and copy its secret.
- Paste the name and secret into the local network's `.env` (`AGENT_NAME`, `AGENT_SECRET`) and start the agent there: `docker compose -f docker-compose-agent.yaml up -d --build`.

### Login
- The user logs in with an access token. This access token is used towards the REST service of BoringProxy
- After successful login the user arrives at the exposed applications.

### Local networks
- Selfie Proxie can only be used for one top domain. This domain is configured in .env and used by Docker Compose. For example example.com.
- Each web service is bound to one subdomain that automatically translates into a FQDN, for example subdoamain music becomes music.example.com
- The Local Networks is integrated in the same page os exposed applications.
- A Local Network corresponds to one Agent (see Agents page). Local Networks are managed there, not on the Exposed Applications page.

### Agents
- Agents are the connecting boringproxy-agent processes (one per Local Network) that open tunnels back to the Selfie Proxy server. Each Agent has a name chosen by the user and a secret generated by the server.
- The Agents page lists all agents. For each agent the user can view/reveal its current secret, generate a new secret (invalidating the old one), rename the agent, or remove it.
- A default agent (name from AGENT_DEFAULT_NAME, "default" unless overridden) is created automatically the first time the admin portal starts, with a freshly generated secret.
- The boringproxy server only accepts connections from agents that exist in this list — an agent's secret is a boringproxy access token scoped to that agent's name, so it can't be used to act as, or register, any other agent.
- To deploy an agent, the user copies its name and secret from this page into the local network's .env (AGENT_NAME, AGENT_SECRET) before starting docker-compose-agent.yaml there.

### Exposed applications
- The top of the page contains a section to manage Local Networks. This section contains a drop down with the name of Local Networks in alphabetic order.
- The first Local Network is automatically selected. 
- The user cannot delete or add Local Networks.
- Under the Local Networks, the exposed applications are listed. These are sorted on subdomain, on alphabet.
- For each exposed app a Name column, the URL to open the service, and the address within the local network are listed. The URL is a link that opens in a new tab. For a Web application the Name column shows the subdomain. For a Network service it shows the Name entered on the edit page instead (the internal svc- subdomain is never shown to the user).
- Next to each exposed app there is an edit button, which opens a the editing page.

### Editing, adding and removing an exposed app
The edit exposed app page is used for adding, editing and deleting exposed app. The following data is shown in the page, in this order:
1. Type: which is Web application (default) or Network service. When the user selects Network service a warning is displayed on the page like Use this at your own risk. Anyone on the internet who scans your domain can see that port is open and attempt to connect to it.
2. Exposed port to the internet (only visible when the type is Network service). Ports 1-1023 are reserved for system services (eg. SSH on 22, HTTPS on 443) and cannot be exposed. A port can only be exposed by one app at a time.
2b. Name (only visible when type is Network service, required). This is just a label shown in the exposed applications list and does not become part of the domain -- it is not unique. The actual subdomain used towards BoringProxy for a Network service is a generated internal svc- value that is never shown to the user.
2. The subdomain (only visible when type is Web application). 
3. The fully qualified domain name which is a label, not a text field. It is displayed immidiately after the subdomain or exposed port. It is the URL in case Type is Web application, and for Network service the Proxy domain + ":" + exposed port. This is not a hyperlink. It is automatically updated when the user changes the data on the page. 
4. Section with the address in the local network. Protocol, host or IP address and port are all displayed on the same line:
-- Protocol. When the Type is Web application then the user chooses in drop down between HTTP and HTTPS. When the Type is Network service the protocol is always TCP.
-- host or IP address. Always shown, for both Web application and Network service (the backend needs it to connect to the local network in both cases).
-- port. Defaults to 80 when the protocol is HTTP, and 443 when the protocol is HTTPS.

When the user selects https as protocol in the local network, then 
- an advanced settings button is shown. Only when user clicks "Advanced settings" then there comes is a section with 3 options for Connectivity between Selfie Proxy and local network is displayed:
1. End-to-end encrypted - (default and recommended) - Selfie Proxy will automatically create and renew signed certificate for you (also known as Server HTTPS)
2. End-to-end encrypted - you provide the certificate from web application in the local network (also known as Client Raw TLS).
3. Hop-by-hop encryption - compatibility mode for web applications that break on normal HTTPS, Selfie Proxy will automatically create and renew signed certificate (also known as Server Raw TLS)

Only when option 2 (Client Raw TLS) is selected, a warning is shown: "Your web application must provide a valid signed certificate from LetsEncrypt. This is typically achieved by using NGINX proxy in the local network."

Below this we find a button panel with following buttons:
- Cancel - returns to the tunnel list without making changes
- OK (in case of add or update)
- Remove (in case of edit) - This button is red background with white text.

Before adding a new exposed app, we check if the domain is already taken (each subdomain can only be used by one app, case-insensitive). This also applies to the internal subdomain generated for a Network Service: it is regenerated until it does not collide with an existing tunnel. Special care must be taken for the BORING_PROXY_ADMIN_SUBDOMAIN and SELFIEPROXY_SUBDOMAIN of the boringproxy server found in .env: a user cannot use these subdomains for their own exposed apps, since they are reserved for the boringproxy admin portal and the Selfie Proxy admin portal itself.
Before removing a tunnel, we ask user for conformation in a overlay.
When changing a exposed app, we will have to remove the boringproxy tunnel, wait 2 seconds, and recreate them using the new values.

## Mapping to boringproxy data model
The first version of Selfie Proxy is using an forked version of BoringProxy. This version is enhanced with: support for WebSockets, exposure of a REST service and support of connecting from client with to applications using without certificates. It also uses ssh-ed25519 instead of ssh-rsa, which is required by current OpenSSL versios, and more secure.  
- The Local Network is called Agent in Boring Proxy.
- A published service is called Tunnel in Boring Proxy. You MUST NOT use words like Client and Tunnel in the portal, because it only adds confusion and complexity.

The following logic is applied when creating a tunnel in BoringProxy:
- An exposed app becomes a tunnel in BoringProxy
- The Domain in BoringProxy is FQDN
- The Agent Name is the name of the Local Network
- The Client Address is the host or IP of the address in the local network
- The Client Port is the port of the address in the local network
- For TLS termination see the advanced options above.

## Integration to boringproxy
- The integration is done through the REST API of the forked BoringProxy. Changes are then written to BoringProxy database and tunnels immediately.

## Prerequisites for the user
In order to use Selfie Proxy you need:
- A domain name with DNS records
- A tiny virtual machine in the Cloud (VPS), costing typically 5$ per month (1 GB memory, 1 vCPU)
- Ubuntu 26.04 and Docker installed on the virtual machine. It may also work with different Linux versions, but that has not been tested.

## Implementation
- The backend is written in Spring 4 and Java with Javascript. The backend does not use Lombok, but used modern Java with records etc.
- Maven project with Java 25
- A Eclipse Project is created.
- Do not use HTTP GET methods to open projects to prevent browser asking "send information to repeat the action".

