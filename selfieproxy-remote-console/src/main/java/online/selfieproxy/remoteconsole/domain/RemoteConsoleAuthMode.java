package online.selfieproxy.remoteconsole.domain;

/** Mirrors selfieproxy-portal's own enum of the same name. PRIVATE_KEY only applies to SSH. */
public enum RemoteConsoleAuthMode {
	PASSWORD,
	PRIVATE_KEY
}
